# Claude Ad Hoc Tasks

Use this folder for explicitly requested one-off tasks that Boss O wants Claude to continue checking outside the normal scheduled workflow.

Rules:
- Create an ad hoc task only when Boss O asks.
- Keep tasks local to Scarlett; do not treat them as general scheduled jobs.
- Heartbeat may check this folder for unfinished work and continue monitoring/reminding when appropriate.
- Mark a task clearly as `open`, `paused`, or `done`.
